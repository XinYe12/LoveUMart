# Uncomment the next line to define a global platform for your project
platform :ios, '12.0' # Updated to iOS 12.0 for better compatibility with newer versions of the Shopify SDK

target 'Love, U Mart' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Love, U Mart
  pod 'Mobile-Buy-SDK'
end
